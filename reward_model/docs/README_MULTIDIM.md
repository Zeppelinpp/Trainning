# 多维度奖励模型训练完整指南

## 📋 概述

本项目实现了一个**多维度评分奖励模型**，采用"黄金标准 + 受控降级"的数据生成方法，在三个核心维度上评估财务分析报告质量：

1. **分析深度 (depth)**: 0-4分
2. **专业度 (professionalism)**: 0-4分  
3. **数值计算准确性 (accuracy)**: 0-4分

使用**交叉熵损失**训练多头分类模型，每个维度独立输出5个档位（0-4）的分类结果。

## 🎯 核心优势

### 与传统Pairwise方法的对比

| 特性 | Pairwise (Chosen/Rejected) | 多维度分类 (本方案) |
|------|---------------------------|-------------------|
| 训练信号 | 相对偏好（A比B好） | 绝对质量（3分/4分） |
| 可解释性 | 低（不知道好在哪） | 高（具体维度得分） |
| 数据利用 | 只用对比对 | Chosen和Rejected都用 |
| 维度细粒度 | 无 | 3个独立维度 × 5个档位 |
| 损失函数 | Ranking Loss | Cross Entropy Loss |
| 应用场景 | 排序、比较 | 评分、诊断、反馈 |

### "黄金标准 + 受控降级"的优势

✅ **信号纯粹**：差异是精确控制的（如只降低深度维度）  
✅ **质量保证**：黄金响应确实高质量，缺陷响应有明确缺陷  
✅ **多样性**：5种质量维度 × 5种降级方式 = 多样化组合  
✅ **可解释**：元数据记录详细的质量维度和降级类型  

## 🔄 完整流程

```
第一阶段：数据生成
├─ 1. 生成分析框架（可选，如已有则跳过）
│   └─ python reward_model/data/synthetic_gen.py
│
├─ 2. 生成对比对数据集
│   ├─ 黄金响应生成（高质量提示词 + 低温度0.3）
│   ├─ 受控降级（降级提示词 + 中温度0.5）
│   └─ 输出: comparison_pairs.jsonl
│
└─ 3. AI裁判多维度打分
    ├─ 使用最强模型（qwen-max）
    ├─ 三维度独立评分（0-4分）
    └─ 输出: comparison_pairs_scored.jsonl

第二阶段：数据验证
└─ 4. 验证数据质量
    ├─ 分数差异统计
    ├─ 标签分布分析
    └─ 问题对比对检测

第三阶段：模型训练
├─ 5. 加载数据集
│   ├─ MultiDimRewardDataset
│   └─ 同时使用Chosen和Rejected样本
│
├─ 6. 训练多头分类模型
│   ├─ 3个独立的分类头
│   ├─ 每头输出5个类别的logits
│   └─ 交叉熵损失
│
└─ 7. 模型评估和保存
    ├─ 整体准确率
    ├─ 每维度准确率和F1
    └─ 最佳模型保存

第四阶段：模型应用
└─ 8. 推理和评分
    ├─ 加载训练好的模型
    └─ 对新报告进行多维度评分
```

## 📦 文件结构

```
reward_model/
├── data/
│   ├── synthetic_gen_v2.py              # 数据生成主脚本
│   ├── validate_multidim_pairs.py        # 数据验证脚本
│   ├── comparison_pairs.jsonl           # 生成的对比对（未打分）
│   └── comparison_pairs_scored.jsonl    # AI裁判打分后的数据
│
├── model.py                              # 模型定义
│   └── FinancialRewardModel            # 多维度分类模型
│
├── dataset_multidim.py                   # 数据集类
│   ├── MultiDimRewardDataset           # 单样本数据集
│   ├── PairwiseMultiDimDataset         # 成对数据集
│   └── get_label_distribution()        # 标签分布分析
│
├── train_multidim.py                     # 训练脚本
│   └── MultiDimRewardTrainer           # 训练器
│
└── inference.py                          # 推理脚本（待创建）
```

## 🚀 快速开始

### 步骤1：生成数据

```bash
# 生成对比对并自动调用AI裁判打分
uv run reward_model/data/synthetic_gen_v2.py
```

配置说明（在脚本的`__main__`中修改）：
- `fields`: 行业列表，默认5个行业
- `n_pairs_per_field`: 每个行业生成的对比对数量，默认10个
- `model_configs`: 生成模型配置，支持多个模型
- `judge_model`: 裁判模型，推荐`qwen-max`

### 步骤2：验证数据

```bash
# 查看数据质量报告
uv run reward_model/data/validate_multidim_pairs.py \
    reward_model/data/comparison_pairs_scored.jsonl

# 同时导出过滤后的高质量数据
uv run reward_model/data/validate_multidim_pairs.py \
    reward_model/data/comparison_pairs_scored.jsonl \
    reward_model/data/filtered_pairs.jsonl
```

验证报告包含：
- ✅ 整体和各维度的分数差异统计
- ✅ 标签分布分析（Chosen vs Rejected）
- ✅ 问题对比对检测
- ✅ 改进建议

### 步骤3：查看数据分布

```bash
# 分析标签分布
uv run python reward_model/dataset_multidim.py \
    reward_model/data/comparison_pairs_scored.jsonl
```

### 步骤4：训练模型

```bash
# 使用默认配置训练
uv run reward_model/train_multidim.py
```

关键训练参数（在`train_multidim.py`的`config`中修改）：
```python
config = {
    "base_model_name": "hfl/chinese-roberta-wwm-ext",  # 基础模型
    "max_length": 2048,           # 最大序列长度
    "batch_size": 4,              # 批次大小
    "learning_rate": 2e-5,        # 学习率
    "num_epochs": 10,             # 训练轮数
    "num_dimensions": 3,          # 维度数
    "num_classes": 5,             # 类别数（0-4）
    "use_multi_head": True,       # 使用多头（推荐）
}
```

训练输出：
- 每个epoch的训练和验证指标
- 整体准确率和各维度准确率
- 各维度的F1分数
- 最佳模型保存在`outputs/multidim/best_model.pt`

## 📊 数据格式

### 输入格式（comparison_pairs_scored.jsonl）

```json
{
  "prompt": "请根据以下分析框架生成制造业的财务分析报告：...",
  "chosen": "黄金响应全文...",
  "rejected": "缺陷响应全文...",
  "metadata": {
    "field": "制造业",
    "model": "qwen-plus",
    "gold_metadata": {
      "quality_focus": "数据准确性导向"
    },
    "defect_metadata": {
      "degradation_type": "浅化深度"
    }
  },
  "scores": {
    "chosen": {
      "depth": 4,
      "professionalism": 3,
      "accuracy": 4
    },
    "rejected": {
      "depth": 1,
      "professionalism": 2,
      "accuracy": 3
    },
    "reasoning": {
      "depth": "黄金响应有深入的归因分析，缺陷响应只有表面描述",
      "professionalism": "黄金响应使用了专业术语，缺陷响应较通用",
      "accuracy": "黄金响应计算精确，缺陷响应有简化"
    }
  }
}
```

### 模型输出格式

```python
outputs = model(input_ids, attention_mask, labels)

# outputs包含:
{
    "logits": torch.Tensor,           # [batch, 3, 5] - 每维度5个类别的logits
    "predicted_labels": torch.Tensor, # [batch, 3] - 预测的类别（0-4）
    "loss": torch.Tensor,            # 交叉熵损失
    "accuracy": float,               # 整体准确率
    "per_dim_accuracy": torch.Tensor # [3] - 各维度准确率
}
```

## 🎓 模型架构

```
FinancialRewardModel
├── Base Model (e.g., RoBERTa)
│   └── Hidden States [batch, seq_len, hidden_size]
│
├── Pooling (Last Token / Mean / CLS)
│   └── Pooled Output [batch, hidden_size]
│
└── Multi-Head Classification (推荐)
    ├── Head 1 (Depth)
    │   ├── Linear(hidden_size, hidden_size//2)
    │   ├── ReLU + Dropout
    │   └── Linear(hidden_size//2, 5) → [batch, 5]
    │
    ├── Head 2 (Professionalism)
    │   └── ... → [batch, 5]
    │
    └── Head 3 (Accuracy)
        └── ... → [batch, 5]
    
    Final Output: [batch, 3, 5]
```

### 损失函数

```python
# 对每个样本的每个维度计算交叉熵
loss = CrossEntropyLoss(
    input=logits.view(-1, 5),      # [batch*3, 5]
    target=labels.view(-1)          # [batch*3]
)

# 相比MSE回归的优势：
# 1. 明确的类别边界（0,1,2,3,4）
# 2. 不受数值大小影响
# 3. 更适合离散评分任务
```

## 📈 评估指标

### 训练过程中

1. **整体准确率**: 所有维度预测正确的比例
2. **各维度准确率**: 每个维度独立的准确率
3. **各维度F1分数**: 宏平均F1（处理类别不平衡）

### 验证数据质量

1. **平均分差**: Chosen和Rejected的平均分差
   - 优秀: ≥1.5分
   - 良好: ≥1.0分
   - 一般: ≥0.5分

2. **正差异率**: Chosen分数 > Rejected分数的比例
   - 优秀: ≥95%
   - 良好: ≥90%
   - 一般: ≥85%

3. **各维度分差**: 每个维度独立的分数差异
   - 应该与降级类型对应（如降级"深度"时，depth维度差异应最大）

## 🔧 高级配置

### 1. 处理类别不平衡

如果某些分数档位样本过少，可以使用类别权重：

```python
# 在model.py的forward中
class_weights = torch.tensor([2.0, 1.5, 1.0, 1.0, 1.0]).to(device)
loss = nn.functional.cross_entropy(
    logits_flat, 
    labels_flat,
    weight=class_weights
)
```

### 2. 使用更大的模型

```python
config = {
    "base_model_name": "THUDM/chatglm3-6b-base",  # 或其他大模型
    ...
}
```

### 3. 增加数据多样性

修改`synthetic_gen_v2.py`中的生成数量：

```python
generate_comparison_dataset(
    fields=["制造业", "服务业", "金融业", "房地产", "科技业"],
    n_pairs_per_field=50,  # 增加到50
    ...
)
```

### 4. 调整评分标准

如果需要更细粒度的评分（如0-9分10档），修改：

```python
# model.py
model = FinancialRewardModel(
    num_classes=10,  # 改为10档
    ...
)

# synthetic_gen_v2.py中的AI裁判提示词
# 将0-4改为0-9，并重新定义每个档位的标准
```

## 🐛 常见问题

### Q1: 训练时显存不足

**解决方案**：
- 减小`batch_size`（如从4改为2或1）
- 减小`max_length`（如从2048改为1024）
- 使用梯度累积：
```python
accumulation_steps = 4
for i, batch in enumerate(train_loader):
    loss = outputs["loss"] / accumulation_steps
    loss.backward()
    if (i + 1) % accumulation_steps == 0:
        optimizer.step()
        optimizer.zero_grad()
```

### Q2: 某个维度准确率很低

**可能原因**：
1. 该维度标签分布极度不平衡
2. 降级策略对该维度影响不明显
3. 基础模型能力不足以区分该维度

**解决方案**：
- 使用类别权重
- 重新设计该维度的降级策略
- 增加该维度的训练样本

### Q3: Chosen和Rejected分数差异小

**原因**：降级不够明显

**解决方案**：
- 检查降级提示词是否足够强
- 增加降级温度（从0.5提高到0.7）
- 人工抽查几个样本，看降级是否生效

### Q4: 生成速度慢/费用高

**解决方案**：
- 使用更便宜的模型（如`qwen-turbo`）
- 减少生成数量
- 使用异步并发生成（修改代码支持）

## 📚 参考资料

### 学术论文

- [Training language models to follow instructions with human feedback (InstructGPT)](https://arxiv.org/abs/2203.02155)
- [Learning to summarize from human feedback](https://arxiv.org/abs/2009.01325)

### 相关项目

- [trl (Transformer Reinforcement Learning)](https://github.com/huggingface/trl)
- [DeepSpeed-Chat](https://github.com/microsoft/DeepSpeedExamples/tree/master/applications/DeepSpeed-Chat)

## 🤝 贡献

欢迎提交Issue和PR！

## 📄 许可

MIT License

---

**祝训练顺利！** 🎉

如有问题，请查看代码注释或提Issue。

